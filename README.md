
# Minaris


## Shooting


file
{
    blocks[
        request{
            name
            input[
                key:val(regex) <- replace in raw
            ]
            output[
                key:val(regex)
            ]
            raw
        }
    ]    
}
